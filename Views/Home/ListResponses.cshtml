@model PartyInvites.Models.GuestResponse[]


@*@model PartyInvites.Controllers.RSVPform(GuestResponse)*@ 

@{
    Layout = null;
}

<html>
<head>
    <title> List of Responses </title>
</head> 
<body>
    
    <h2> your the @ViewBag.Count person to RSVP!  </h2>
    <h1> @FakeDb.GetResponseCount() people coming </h1> 


    @for (int i = 0; i < Model.Length; i++)
    {
        if (Model[i] != null)
        {
            @: <tr>
                @: <td> @Model[i].Name </td>
                @: <td> @Model[i].Email </td>
                @: <td> @Model[i].Phone </td>
                @: <td> @Model[i].WillAttend </td>
            @: </tr>








        }
    }

    </ol>
</body>
</html>